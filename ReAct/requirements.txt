# The ReAct demo is pure Python standard library (an agent loop over two toy
# tools) — it runs instantly with no tensors. numpy is listed as the only
# optional dependency for anyone extending the analysis.
#
# Optionally install into the shared CPU environment with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
numpy>=1.26

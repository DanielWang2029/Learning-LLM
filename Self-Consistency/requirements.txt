# The Self-Consistency demo is pure Python standard library (no training loop,
# no tensors) — it runs in well under a second. numpy is listed as the only
# optional dependency for anyone extending the analysis.
#
# Optionally install into the shared CPU environment with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
numpy>=1.26
